{
  "email": {
    "required": "Email is required.",
    "invalid": "Invalid email format."
  },
  "password": {
    "required": "Password is required.",
    "tooShort": "Password must be at least {0} characters.",
    "tooWeak": "Password is too weak."
  },
  "firstName": {
    "maxLength": "First name cannot exceed {0} characters."
  },
  "lastName": {
    "maxLength": "Last name cannot exceed {0} characters."
  },
  "displayName": {
    "maxLength": "Display name cannot exceed {0} characters."
  },
  "userId": {
    "required": "User ID is required."
  },
  "roleId": {
    "required": "Role ID is required."
  },
  "roleName": {
    "required": "Role name is required.",
    "minLength": "Role name must be at least {0} characters.",
    "maxLength": "Role name cannot exceed {0} characters.",
    "pattern": "Role name must start with a letter and contain only letters, numbers, underscores, and hyphens.",
    "empty": "Role name cannot be empty."
  },
  "rolesList": {
    "required": "Roles list is required."
  },
  "permissions": {
    "required": "Permissions list is required.",
    "minOne": "At least one permission must be specified.",
    "empty": "Permission cannot be empty.",
    "invalid": "Invalid permission: {0}."
  },
  "accessToken": {
    "required": "Access token is required."
  },
  "refreshToken": {
    "required": "Refresh token is required when not using cookies."
  },
  "correlationId": {
    "required": "Correlation ID is required."
  },
  "entityType": {
    "required": "Entity type and entity ID are required."
  },
  "maxRows": {
    "positive": "Max rows must be greater than 0."
  },
  "dateRange": {
    "exceeded": "Date range cannot exceed {0} days. Requested: {1} days.",
    "invalid": "From date must be before to date.",
    "filterRequired": "At least one filter (from date, to date, entity type, or user ID) is required."
  }
}
